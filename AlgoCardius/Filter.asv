function [FilteredData] = Filter(Data2Filter)

Ydata = zeros(1,3);
Udata = zeros(1,3);
FilteredData = 

for index = 1:2500
    Udata(3) = Udata(2);
    Udata(2) = Udata(1);
    Udata(1) = Data2Filter(index);
    
    
    
    
end

